{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE"
  },
  "deploy": {
    "startCommand": "sh -c 'node /app/scripts/write-key.mjs && export OPENCLAW_CONFIG_PATH=/app/openclaw.json && node /app/openclaw.mjs gateway run --bind lan --port $PORT --allow-unconfigured'",
    "healthcheckPath": "/healthz",
    "healthcheckTimeout": 30,
    "restartPolicyType": "ON_FAILURE"
  },
  "variables": {
    "OPENCLAW__EXTENSIONS__CODEX__DISCOVERY__ENABLED": "false"
  }
}
